version: '3.8'
services:
  postgres:
    image: postgres:latest
    container_name: postgres
    environment:
      POSTGRES_USER: TroveUser001
      POSTGRES_PASSWORD: TroveHas&&$99989
      POSTGRES_DB: trovedb
    ports:
      - "5432:5432"
    volumes:
      - ./pgdata:/var/lib/postgresql/data
